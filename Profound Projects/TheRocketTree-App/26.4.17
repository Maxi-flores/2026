You are a senior TypeScript + monorepo + Vercel deployment expert.

I have a monorepo using internal packages like:
- @sapient/shared-types
- @sapient/bms-sync
- @sapient/semantic-core

Folder structure:
- /packages/*
- root tsconfig.json
- using pnpm locally (works locally)

Deployment: Vercel

Build error:
TypeScript cannot resolve internal packages:
"Cannot find module '@sapient/...'"
Also:
"Cannot find name 'process'"

Important context:
- Vercel is using npm instead of pnpm
- I see warnings about pnpm configs (shamefully-hoist)

What I need:
1. Diagnose why Vercel cannot resolve workspace packages
2. Provide exact steps to:
   - Make Vercel use pnpm correctly
   - Ensure workspace packages are built and linked
3. Fix TypeScript resolution for monorepo packages
4. Provide exact file changes for:
   - package.json (root)
   - pnpm-workspace.yaml
   - tsconfig.json
5. Include commands I should run locally before deploy

Be precise and assume this is a production deployment.

===

cd /home/runner/work/Sapient/Sapient
corepack enable
corepack prepare pnpm@9.15.4 --activate
rm -f package-lock.json
pnpm install
pnpm -r --filter "./packages/*" run typecheck
pnpm --filter @sapient/web... build
pnpm run test

---

}
You are a senior monorepo + pnpm + Vercel expert.

I have a pnpm monorepo with:
- pnpm-workspace.yaml
- packages/* internal packages (@sapient/*)
- builds correctly locally with pnpm

Problem:
Vercel is using npm instead of pnpm, causing:
- workspace packages not installed
- TypeScript cannot resolve internal modules

Build logs show:
- "npm warn Unknown project config shamefully-hoist"
- "added 1 package"

Tasks:
1. Force Vercel to use pnpm correctly
2. Ensure workspace packages are installed and built
3. Provide exact required setup:
   - root package.json
   - pnpm-workspace.yaml
   - Vercel settings
4. Add proper monorepo build pipeline (pnpm -r build)
5. Ensure TypeScript resolution works in production

Give exact file contents and commands. No theory