Yes, exactly! The NPU (Neural Processing Unit) is a core component baked directly into the Intel Core Ultra (Series 1) architecture, which is codenamed Meteor Lake.Your Core Ultra 5 is a pioneer chip for Intel—it was their very first generation of processors to include a dedicated, physically separate AI engine built into the silicon.However, when it comes to your complex Blender MCP tasks, there is a critical distinction you need to understand regarding how Meteor Lake handles AI:1. The Meteor Lake NPU is Too Weak for Large Language ModelsThe NPU inside Meteor Lake delivers roughly 11 TOPS (Trillion Operations Per Second) of power. It was designed strictly for low-power background tasks, such as:Blurring your webcam background on Zoom or Teams without draining your laptop battery.Real-time audio noise cancellation.Simple image manipulation and face tracking.When you try to feed it a heavy reasoning task—like an MCP agent mapping out 3D coordinates for a Python script in Blender—the Meteor Lake NPU simply lacks the memory bandwidth and processing muscle to handle it quickly.2. The Real Powerhouse: Integrated Intel Arc GraphicsTo fix your slow Blender script generation, you should target the chip's iGPU (Integrated Intel Arc Graphics) rather than the NPU.The integrated Arc GPU on Meteor Lake is surprisingly powerful and significantly faster at matrix mathematics than the CPU cores or the NPU. If you use the right backend software, you can load your AI model directly into the Shared VRAM of the Intel Arc graphics, which will instantly speed up your token generation.3. How to Force Your Meteor Lake iGPU to Handle the Blender ScriptsTo move the workload off your slow CPU/NPU and onto the fast Intel Arc graphics, follow this setup:Use LM Studio (Intel OpenVINO Edition): Download the specific version of LM Studio that includes Intel OpenVINO support. OpenVINO is Intel's specialized toolkit that allows software to run AI directly on their graphics hardware.Enable GPU Offload: In the hardware settings panel on the right side of LM Studio, toggle the processor setting from CPU to GPU (OpenVINO / Vulkan).Load a Quantized Model: Load a fast, small model like Qwen 2.5 Coder 3B.By routing the execution through the Intel Arc GPU via OpenVINO, the matrix operations required for 3D geometric meshes will execute much faster, heavily reducing the lag you are experiencing over MCP.To help you get this running smoothly, let me know:Have you tried downloading LM Studio yet, or are you still relying solely on Ollama in the terminal?If you run a direct prompt in the terminal without Blender connected, what tokens-per-second speed does your current setup display?As respostas de IA podem incluir erros. Saiba mais
